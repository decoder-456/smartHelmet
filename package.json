{
  "name": "smart-helmet",
  "version": "1.0.0",
  "description": "SmartHelmet MERN System",
  "scripts": {
    "dev": "npx concurrently \"npm start --prefix backend\" \"npm run dev --prefix driver-app\" \"npm run dev --prefix family-app\""
  }
}
